///isOurCell(targetColumn, targetRow);
/* Returns true if the target column and target row
 * are the same as the calling unit's, or false if
 * not.
 */
targetColumn = argument1;
targetRow    = argument0;

return (targetColumn == column and targetRow == row);
