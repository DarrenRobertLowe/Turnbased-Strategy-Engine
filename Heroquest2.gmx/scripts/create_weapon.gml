/// create_weapon(object, name, pwr, dice, range, effects, modifiers)
/* This will create a new instance of type object
 * with the stats as given.
 *
 * For basic weapons you can simply
 * use weapon = instance_create(Cutlass) etc
 */

/*
obj     = ds_grid_get(weaponsGrid, weaponStats.type);

weapon  = instance_create(0,0, object);

weapon.name         = ds_grid_get(weaponsGrid, weaponStats.name);
weapon.pwr          = ds_grid_get(weaponsGrid, weaponStats.pwr);
weapon.dice         = ds_grid_get(weaponsGrid, weaponStats.dice);
weapon.range        = ds_grid_get(weaponsGrid, weaponStats.range);
weapon.modifiers    = ds_grid_get(weaponsGrid, weaponStats.modifiers);



return weapon;
*/
